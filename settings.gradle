// TODO drop this; for one thing use the newer script; for another, use a dedicated property with descriptive name
// note: ./scripts/build.sh detects the project name from the first include
include ':winenotes'
// TODO winenotes-tests ?
